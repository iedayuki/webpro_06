<!Doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title><%= data.name %></title>
  <style>
    body{margin:0;font-family:system-ui,-apple-system,"Segoe UI",Roboto,"Hiragino Kaku Gothic ProN","Noto Sans JP","Yu Gothic",sans-serif;background:#f6f7fb;color:#222;}
    .wrap{max-width:720px;margin:48px auto;padding:0 16px;}
    .card{background:#fff;border:1px solid #e6e8ef;border-radius:16px;padding:24px;box-shadow:0 8px 24px rgba(0,0,0,0.06);}
    h1{font-size:20px;margin:0 0 6px 0;}
    .sub{margin:0 0 16px 0;font-size:13px;color:#555;}
    table{width:100%;border-collapse:collapse;overflow:hidden;border-radius:12px;}
    th,td{padding:12px 10px;border-bottom:1px solid #eef0f6;font-size:14px;}
    th{text-align:left;background:#f3f5fb;width:38%;}
    input[type="text"]{width:100%;padding:10px 12px;border:1px solid #d7dbe7;border-radius:10px;background:#fff;outline:none;font-size:14px;box-sizing:border-box;}
    input[type="text"]:focus{border-color:#7aa7ff;box-shadow:0 0 0 4px rgba(122,167,255,0.2);}
    .actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:16px;justify-content:flex-end;align-items:center;}
    input[type="submit"]{appearance:none;border:0;border-radius:12px;padding:10px 14px;background:#2f6fed;color:#fff;font-weight:600;cursor:pointer;box-shadow:0 8px 18px rgba(47,111,237,0.25);}
    .btn{display:inline-block;border-radius:12px;padding:10px 14px;text-decoration:none;font-weight:600;border:1px solid #d7dbe7;color:#333;background:#fff;}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1><%= data.name %> の編集</h1>
      <p class="sub">内容を変更して登録してください。</p>

      <form action="/onsen_chiba/update/<%= id %>" method="POST">
        <table>
          <tr><th>項目</th><th>データ</th></tr>
          <tr><td>ID</td><td><%= data.id %></td></tr>
          <tr><td>施設名</td><td><input type="text" name="name" value="<%= data.name %>"></td></tr>
          <tr><td>市区町村</td><td><input type="text" name="city" value="<%= data.city %>"></td></tr>
          <tr><td>料金</td><td><input type="text" name="fee" value="<%= data.fee %>"></td></tr>
          <tr><td>営業時間</td><td><input type="text" name="hours" value="<%= data.hours %>"></td></tr>
          <tr><td>定休日</td><td><input type="text" name="closed" value="<%= data.closed %>"></td></tr>
          <tr><td>メモ</td><td><input type="text" name="note" value="<%= data.note %>"></td></tr>
        </table>

        <div class="actions">
          <input type="submit" value="登録">
          <a class="btn" href="/onsen_chiba/">一覧に戻る</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
